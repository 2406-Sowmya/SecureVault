# SecureVault Backend – Python dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9      # required for file uploads

# Security & auth
bcrypt>=4.1.2
python-jose[cryptography]>=3.3.0   # JWT
passlib>=1.7.4

# Face recognition (requires cmake + dlib C++ build tools)
opencv-python>=4.9.0
face-recognition>=1.3.0
Pillow>=10.3.0
numpy>=1.26.0

# Email
# (smtplib is stdlib – no pip install needed)

# Env + HTTP
python-dotenv>=1.0.0
requests>=2.31.0

# Pydantic email validation
pydantic[email]>=2.7.0
